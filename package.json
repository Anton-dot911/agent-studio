{
  "name": "agent-studio",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=apps/web",
    "dev": "npm run dev --workspace=apps/web"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.8.0"
  },
  "engines": { "node": ">=20" }
}
