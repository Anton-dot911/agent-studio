# Dependencies
node_modules
.pnpm-store

# Next.js
.next
out
next-env.d.ts

# TypeScript build cache
*.tsbuildinfo

# Lock files
# apps/web/package-lock.json is committed intentionally so Netlify builds are
# reproducible via `npm ci` (prevents transitive dependency drift). Other lock
# files remain ignored.
pnpm-lock.yaml

# Build outputs
dist
build

# Environment
.env
.env.local
.env.*.local

# Turbo
.turbo

# Logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# IDE
.vscode
.idea

# Netlify
.netlify
